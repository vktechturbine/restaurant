import React from 'react';
import Login from '../components/Login';
import Slider from '../components/Slider';

const page = () => {
  return (
    <div>
      <Slider title="Login"/>
      <Login/>
    </div>
  );
}

export default page;
